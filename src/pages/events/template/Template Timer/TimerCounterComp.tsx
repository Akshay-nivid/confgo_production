import Grid  from "@mui/material/Grid2";
import React from "react";

const TimerCounterComp:React.FC<any> = React.memo(({})=>{
    return(
        <Grid>
            
        </Grid>
    )
})

export default TimerCounterComp;